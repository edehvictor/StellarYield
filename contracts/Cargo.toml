[workspace]
resolver = "2"
members = ["yield_vault", "zap", "options", "aa_recovery", "aa_factory", "settlement", "intent_swap", "dutch_auction", "liquid_staking", "emission_controller", "bridge_relayer"]

[profile.release]
opt-level = "z"
overflow-checks = true
lto = true
